Oprocs is a GUI tool for running multiple commands in parallel and showing output of each command separately.

It's aim is to be compatible with mprocs and mprocs.yaml files, but provide a more comfortable and powerful experience through its GUI.
